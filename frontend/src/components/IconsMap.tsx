import { FilePen, Atom, CodeXml, Languages, Database, Calculator, Notebook, Hamburger } from 'lucide-react'
import type { LucideIcon } from 'lucide-react'

export const iconMap: Record<string, LucideIcon> = {
    FilePen,
    Atom,
    CodeXml,
    Languages,
    Database,
    Calculator,
    Notebook,
    Hamburger,
}